services:
  db:
    image: postgres:18
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: test
    ports:
      - "5433:5433"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
